[
    {
        "ghsa": "GHSA-xcpc-8h2w-3j85",
        "reason": "adm-zip <0.6.0 memory-exhaustion on a crafted ZIP. Three copies sit in word-addin, all inside the vulnerable range and all devDependency tooling used to sideload the add-in on a developer's machine: office-addin-debugging 6.1.2, office-addin-dev-settings and office-addin-project pin 0.5.12 exactly, office-addin-manifest 2.1.6 pins 0.5.16, and @microsoft/kiota wants ^0.5.17 (0.5.18). Never bundled by webpack, never part of the shipped task pane, and the only ZIPs it opens are the manifest bundles we produce ourselves. The pins are exact, so npm update cannot move them and no in-range fix exists. REMOVE WHEN: every dependent resolves adm-zip >=0.6.0 — check with `npm ls adm-zip` in word-addin (the weekly dev-dep Dependabot bump will carry it)."
    },
    {
        "ghsa": "GHSA-rgj7-g3m4-5g8c",
        "reason": "sharp 0.35.2 bundles a libheif with two HEIF-decoding CVEs (GHSA-g89c-p67h-r497, GHSA-2jg2-4ch7-h545). The vulnerable copy is the one miniflare pins EXACTLY (miniflare -> sharp 0.35.2), reached in the frontend only through wrangler, i.e. the local Cloudflare emulator behind `wrangler dev` / `opennextjs-cloudflare preview` on a developer's machine. It is not part of the deployed Worker or the Next.js bundle; the sharp that Next itself uses for image optimisation is already 0.35.4 (patched). No in-range fix exists: miniflare@latest (5.20260908.0-alpha, shipped by wrangler 4.130.0) still pins 0.35.2, and the pin is exact so npm update cannot move it. REMOVE WHEN: `npm view miniflare dependencies.sharp` reports >=0.35.4 and the frontend's wrangler resolves that miniflare (the weekly dev-dep Dependabot bump will carry it)."
    }
]
